# 🏠 홈 서버 배포 가이드 (집 노트북 활용하기)

이 가이드는 집에서 노는 노트북을 활용하여 본 블로그 서비스를 무중단 배포하고, 외부(구글 크롤러 및 사용자)에서 접속할 수 있도록 네트워크를 세팅하는 방법을 설명합니다.

---

## 1. 서버용 노트북 사전 준비

### 1-1. Node.js & MySQL 설치
- 개발 PC와 마찬가지로, 배포 대상 노트북에도 **Node.js(v20 이상)** 및 **MySQL(v8.0 이상)**이 설치되어 있어야 합니다.
- 데이터베이스 백업 및 격리를 위해 노트북에 **Docker Desktop**을 깔고 Docker Compose로 MySQL을 구동하는 것도 좋은 방법입니다. (루트의 `docker-compose.yml` 참고)

### 1-2. 무중단 프로세스 관리자 (PM2) 설치
노트북 터미널을 닫아도 Next.js 웹 서버가 계속 배경에서 실행되도록 PM2를 설치합니다.
```bash
npm install -g pm2
```

---

## 2. 블로그 애플리케이션 빌드 및 실행

### 2-1. 환경 변수 세팅 (`.env`)
노트북에 프로젝트 소스코드를 복사한 뒤, 루트 디렉터리에 `.env` 파일을 생성합니다.
```env
# 데이터베이스 주소 (노트북 MySQL 계정 정보 입력)
DATABASE_URL="mysql://root:aa152535@127.0.0.1:3306/blogdb"

# 외부 공개 도메인 주소 (구글 애드센스 및 Sitemap 생성용)
NEXT_PUBLIC_SITE_URL="http://[본인의 개인 도메인 또는 DDNS 주소]"
```

### 2-2. 빌드 및 PM2 실행
```bash
# 1. 의존성 패키지 설치
npm install

# 2. Prisma 클라이언트 생성 및 DB 마이그레이션 실행
npx prisma migrate dev --name init

# 3. Next.js 빌드 (서버 사이드 렌더링 최적화 파일 생성)
npm run build

# 4. PM2로 웹 서버 무중단 구동
pm2 start npm --name "aesthetic-blog" -- start
```

- 서버 상태 확인: `pm2 status`
- 실시간 로그 보기: `pm2 logs`
- 서버 재시작: `pm2 restart aesthetic-blog`

---

## 3. 네트워크 설정 (외부 접속 가능케 하기)

집 안의 공유기 환경(Wi-Fi) 내에 있는 노트북은 기본적으로 사설 IP를 쓰므로 외부에서 바로 들어올 수 없습니다. 아래 3단계를 수행해야 합니다.

### 3-1. 노트북 사설 IP 고정
노트북의 IP 주소가 공유기 재부팅 시 바뀌지 않도록 고정합니다.
- **Windows**: 제어판 -> 네트워크 및 공유 센터 -> 어댑터 설정 변경 -> 사용 중인 네트워크 우클릭 -> 속성 -> `Internet Protocol Version 4 (TCP/IPv4)` 선택 -> 다음 IP 주소 사용 선택 후 고정 IP 등록 (예: `192.168.0.100`)

### 3-2. 공유기 DDNS 설정 (도메인 고정)
가정용 인터넷은 공인 IP가 유동적으로 변합니다. 이를 해결하기 위해 도메인을 고정해주는 DDNS 서비스를 설정합니다.
- **iptime 공유기 기준**:
  1. 브라우저에 `192.168.0.1` 접속 후 로그인.
  2. 고급 설정 -> 특수기능 -> **DDNS 설정** 이동.
  3. 호스트이름(예: `myaesthetic.iptime.org`)과 사용자 이메일을 넣고 등록.
- **DuckDNS (무료 도메인) 사용 시**: [DuckDNS](https://www.duckdns.org) 가입 후 서브도메인을 파서 연결할 수 있습니다.

### 3-3. 포트포워딩 설정
외부에서 공유기로 들어온 요청을 노트북의 Next.js 포트(기본 `3000`번)로 연결해주는 작업입니다.
- **iptime 공유기 기준**:
  1. 고급 설정 -> NAT/라우터 관리 -> **포트포워드 설정** 이동.
  2. 새 규칙 추가:
     - **규칙 이름**: Blog Web
     - **내부 IP 주소**: [3-1에서 고정한 노트북 IP 입력] (예: `192.168.0.100`)
     - **프로토콜**: TCP
     - **외부 포트**: `80` (HTTP 기본 포트. 주소 뒤에 `:3000` 안 붙여도 되게 함)
     - **내부 포트**: `3000` (Next.js 포트)
  3. 적용 및 저장.

### 3-4. 윈도우 방화벽 인바운드 허용
노트북 자체 방화벽에서 3000포트를 열어주어야 접속이 됩니다.
- Windows 검색창에 `고급 보안이 포함된 Windows 방화벽` 실행 -> **인바운드 규칙** -> **새 규칙** -> **포트** 선택 -> **TCP/특정 로컬 포트 (3000)** 입력 -> **연결 허용** -> 규칙 이름(예: `Next.js Port`) 지정 후 완료.

이제 브라우저에 `http://[설정한 DDNS 호스트]`를 입력해 접속이 되는지 테스트합니다.

---

## 4. 구글 애드센스 팁 & 개인 도메인 구매 추천

구글 애드센스 광고는 `xxx.iptime.org` 또는 `xxx.duckdns.org` 같은 **공용 서브도메인 주소에는 승인을 잘 내주지 않거나 불가능**합니다.
따라서 가비아, 후이즈 등에서 **1년에 약 1~2만 원 선의 개인 도메인(예: `myaesthetic.com`)**을 구입하시는 것을 강력히 추천합니다.

### 개인 도메인을 구매했다면?
도메인 판매처의 DNS 관리 페이지에서 **CNAME** 또는 **A 레코드**를 설정합니다.
- **CNAME 설정**: `myaesthetic.com` 도메인의 타겟을 본인의 DDNS 주소(`myaesthetic.iptime.org`)로 매핑합니다.
- 매핑이 완료되면, 사용자들은 `http://myaesthetic.com` 주소로 깔끔하게 접속하게 되며, 구글 애드센스 신청도 정상 승인될 수 있는 요건이 갖춰집니다.
